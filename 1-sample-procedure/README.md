In this simple assembly program, I demonstrate how to create a simple procedure (procedure is what a function is in other programming languages) in assembly.
There is a procedure named `sampleProc` which upon being called returns the control immediately back to the caller which is this case is `main`.
